# GitHub App credentials — from your GitHub App settings page
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here

# Port to listen on (default: 3000)
PORT=3000

# Absolute path to the references/ folder.
# Default resolves to ../../references relative to this extension directory,
# which is correct when running locally inside this workspace.
# Override when deploying to a remote server.
REFERENCES_PATH=

# (Optional) Your GitHub repo in owner/repo format — used by the get_reference
# skill to fetch reference files via the GitHub API instead of the filesystem.
# Example: my-org/java-backend-copilot
GITHUB_REPO=
